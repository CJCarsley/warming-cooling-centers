# Dependencies
node_modules/
amplify/node_modules/

# Build output
dist/
.amplify/

# Amplify outputs — regenerated by `ampx pipeline-deploy` during CI/CD
# Committed here only for local sandbox convenience; production values are injected at build time
# amplify_outputs.json   ← intentionally NOT ignored so local dev works out of the box

# Environment / secrets
.env
.env.local
.env.*.local

# Editor
.vscode/
.idea/
*.suo
*.user

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
